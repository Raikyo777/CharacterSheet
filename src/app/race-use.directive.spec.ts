import { RaceUseDirective } from './race-use.directive';

describe('RaceUseDirective', () => {
  it('should create an instance', () => {
    const directive = new RaceUseDirective();
    expect(directive).toBeTruthy();
  });
});
